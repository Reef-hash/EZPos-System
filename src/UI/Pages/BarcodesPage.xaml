<UserControl x:Class="EZPos.UI.Pages.BarcodesPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:fa="clr-namespace:FontAwesome.Sharp;assembly=FontAwesome.Sharp"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="{StaticResource DashboardBackgroundBrush}">

    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- ── Header ── -->
        <materialDesign:Card Grid.Row="0" Padding="22,18" Margin="0,0,0,16" Background="{StaticResource DashboardSurfaceBrush}">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Border Width="4" Height="26" CornerRadius="2" Margin="0,0,14,0" Background="{StaticResource DashboardPrimaryBrush}" />
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Barcodes" Foreground="{StaticResource DashboardTextPrimaryBrush}" FontSize="20" FontWeight="Bold" />
                        <Border Background="{StaticResource DashboardPrimaryBrush}" CornerRadius="4" Margin="8,4,0,0" Padding="5,2" VerticalAlignment="Center">
                            <TextBlock Text="BETA" FontSize="9" FontWeight="Bold" Foreground="#111014" />
                        </Border>
                    </StackPanel>
                    <TextBlock Text="Generate, preview, and print product barcode labels." Foreground="{StaticResource DashboardTextSecondaryBrush}" FontSize="12" Margin="0,4,0,0" />
                </StackPanel>
            </StackPanel>
        </materialDesign:Card>

        <!-- ── Two-panel body ── -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>

            <!-- ── Left panel: product selection ── -->
            <materialDesign:Card Grid.Column="0" Padding="18" Background="{StaticResource DashboardSurfaceBrush}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBox x:Name="SearchBox" Width="200" Height="34" Margin="0,0,8,0"
                                 VerticalContentAlignment="Center"
                                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
                        <ComboBox Width="160" Height="34"
                                  ItemsSource="{Binding Categories}"
                                  SelectedItem="{Binding SelectedCategory}" />
                    </StackPanel>

                    <DataGrid Grid.Row="1"
                              Style="{StaticResource DashboardDataGrid}"
                              ItemsSource="{Binding FilteredProducts}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              HorizontalScrollBarVisibility="Auto"
                              VerticalScrollBarVisibility="Auto">
                        <DataGrid.Columns>
                            <DataGridCheckBoxColumn Header="" Binding="{Binding IsSelected, Mode=TwoWay}" Width="40" />
                            <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="2*" IsReadOnly="True" ElementStyle="{StaticResource DgCellText}" />
                            <DataGridTextColumn Header="Barcode" Binding="{Binding Barcode}" Width="1.4*" IsReadOnly="True" ElementStyle="{StaticResource DgCellText}" />
                            <DataGridTextColumn Header="Format" Binding="{Binding Format}" Width="1*" IsReadOnly="True" ElementStyle="{StaticResource DgCellText}" />
                        </DataGrid.Columns>
                    </DataGrid>

                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,0">
                        <Button Style="{StaticResource SecondaryButtonStyle}" Padding="12,8" Margin="0,0,8,0"
                                Content="Select All" Command="{Binding SelectAllCommand}" />
                        <Button Style="{StaticResource SecondaryButtonStyle}" Padding="12,8"
                                Content="Select by Category" Command="{Binding SelectByCategoryCommand}" />
                    </StackPanel>
                </Grid>
            </materialDesign:Card>

            <!-- ── Right panel: template config + preview + print ── -->
            <materialDesign:Card Grid.Column="2" Padding="18" Background="{StaticResource DashboardSurfaceBrush}">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel>

                    <!-- Template / Format / Printer -->
                    <Grid Margin="0,0,0,14">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="10" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="10" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Template" Foreground="{StaticResource DashboardTextSecondaryBrush}" FontSize="12" Margin="0,0,0,4" />
                            <ComboBox Height="34" ItemsSource="{Binding Templates}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedTemplate}" />
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Format" Foreground="{StaticResource DashboardTextSecondaryBrush}" FontSize="12" Margin="0,0,0,4" />
                            <ComboBox Height="34" ItemsSource="{Binding AvailableFormats}" SelectedItem="{Binding SelectedFormat}" />
                        </StackPanel>

                        <StackPanel Grid.Column="4">
                            <TextBlock Text="Printer" Foreground="{StaticResource DashboardTextSecondaryBrush}" FontSize="12" Margin="0,0,0,4" />
                            <ComboBox Height="34" ItemsSource="{Binding Printers}" SelectedItem="{Binding SelectedPrinter}" />
                        </StackPanel>
                    </Grid>

                    <!-- Field toggles -->
                    <TextBlock Text="LABEL FIELDS" Foreground="{StaticResource DashboardTextSecondaryBrush}" FontSize="10" FontWeight="SemiBold" Margin="0,0,0,8" />
                    <WrapPanel Margin="0,0,0,16">
                        <CheckBox Content="Barcode" Margin="0,0,16,8" IsChecked="{Binding SelectedTemplate.ShowBarcode}" />
                        <CheckBox Content="Name" Margin="0,0,16,8" IsChecked="{Binding SelectedTemplate.ShowName}" />
                        <CheckBox Content="Price" Margin="0,0,16,8" IsChecked="{Binding SelectedTemplate.ShowPrice}" />
                        <CheckBox Content="Category" Margin="0,0,16,8" IsChecked="{Binding SelectedTemplate.ShowCategory}" />
                        <CheckBox Content="Store Name" Margin="0,0,16,8" IsChecked="{Binding SelectedTemplate.ShowStoreName}" />
                    </WrapPanel>

                    <!-- Quantity list -->
                    <TextBlock Text="QUANTITY" Foreground="{StaticResource DashboardTextSecondaryBrush}" FontSize="10" FontWeight="SemiBold" Margin="0,0,0,8" />
                    <Border BorderBrush="{StaticResource DashboardBorderBrush}" BorderThickness="1" CornerRadius="4" Margin="0,0,0,16" MaxHeight="180">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding PrintJobs}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="10,6">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="70" />
                                            <ColumnDefinition Width="34" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding ProductName}"
                                                   Foreground="{StaticResource DashboardTextPrimaryBrush}"
                                                   VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                                        <TextBox Grid.Column="1" Height="30" VerticalContentAlignment="Center"
                                                 Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}" />
                                        <Button Grid.Column="2" Width="30" Height="30" Padding="0"
                                                Style="{StaticResource SecondaryButtonStyle}"
                                                Command="{Binding DataContext.RemoveJobCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}">
                                            <fa:IconBlock Icon="Xmark" FontSize="12" />
                                        </Button>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        </ScrollViewer>
                    </Border>

                    <!-- Preview -->
                    <TextBlock Text="PREVIEW" Foreground="{StaticResource DashboardTextSecondaryBrush}" FontSize="10" FontWeight="SemiBold" Margin="0,0,0,8" />
                    <Border BorderBrush="{StaticResource DashboardBorderBrush}" BorderThickness="1" CornerRadius="4" Padding="14" Margin="0,0,0,16" Background="White">
                        <StackPanel HorizontalAlignment="Center">
                            <Image Source="{Binding PreviewImage}" Height="80" Stretch="Uniform" />
                            <TextBlock Text="{Binding PreviewJob.ProductName}" HorizontalAlignment="Center" Foreground="Black" FontSize="12" Margin="0,6,0,0" />
                            <TextBlock Text="{Binding PreviewJob.Price, StringFormat='RM {0:F2}'}" HorizontalAlignment="Center" Foreground="Black" FontSize="13" FontWeight="Bold" />
                        </StackPanel>
                    </Border>

                    <!-- Print -->
                    <Button Style="{StaticResource DashboardRaisedButton}" Height="42" Command="{Binding PrintCommand}">
                        <StackPanel Orientation="Horizontal">
                            <fa:IconBlock Icon="Print" FontSize="14" Margin="0,0,8,0" />
                            <TextBlock Text="Print Labels" />
                        </StackPanel>
                    </Button>

                </StackPanel>
                </ScrollViewer>
            </materialDesign:Card>
        </Grid>
    </Grid>
</UserControl>
